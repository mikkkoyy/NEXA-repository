// Economy configuration constants.
// These are the fixed values for the NEXA economy system.

// Currency name and symbol.
const CurrencyName = 'NEXA Coin';
const CurrencySymbol = '🪙';

// Starting balance for new members.
const StartingBalance = 0;

module.exports = {
  CurrencyName,
  CurrencySymbol,
  StartingBalance
};